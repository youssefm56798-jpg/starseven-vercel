<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" role="img" aria-hidden="true">
  <!-- Low density is drawn as absence: three strands, the thinnest strokes in the set, held far apart so the empty ground between them is most of the mark. The middle one is shorter and thinner again, which is what thinning actually looks like. -->
  <!-- currentColor, resolved to ink by the color attribute below, so the file still
       reads as an ink drawing when it is loaded as an <img> on the hair-type pages,
       which inherit nothing from the page around them. On the home tiles the same
       file is a CSS mask, where only the alpha matters and the tile paints it. -->
  <g fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" color="#12100B">
    <path stroke-width="2.2" d="M17.8 9.2C15.8 18 18.6 27.4 16.2 40.2"/>
    <path stroke-width="2.2" d="M31 9.6C33.2 18.4 30 27.8 32.4 40.6"/>
    <path stroke-width="1.7" d="M24.4 14.6C23.2 22.4 25.2 30.2 24 36.8"/>
  </g>
</svg>
